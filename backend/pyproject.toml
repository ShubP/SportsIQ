[project]
name = "courtiq-backend"
version = "0.1.0"
description = "CourtIQ MLB edge-prediction backend, data pipeline, and ML models"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
ignore = ["E501"]

[tool.flake8]
max-line-length = 100
